require(UsingR)

data(kid.weights)
data("michelson")
data("fat")
data("MASS")
data("ToothGrowth") #not part of UsingR
attach(kid.weights)
attach(michelson)
attach(fat)
# use data() to see all datasets


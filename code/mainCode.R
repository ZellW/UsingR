require(dplyr)
require(UsingR)

data(kid.weights)
data("michelson")
data("fat")
data("SAT")
data("heartrate")
#data("MASS")
data("ToothGrowth") #not part of UsingR
attach(kid.weights)
attach(michelson)
attach(fat)
attach(SAT)
attach(heartrate)
# use data() to see all datasets

